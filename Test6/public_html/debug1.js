//This program computes the sum of 2 given integers.
//If the two values are the same, then it returns triple their sum.

var number1 = prompt("Enter the first number");
var number2 = prompt("Enter the second number")

var sum = number1 + number2;
if (number1 = number2) {
    document.write(3 * sum);
}
else 
    document.write (sum);
}

